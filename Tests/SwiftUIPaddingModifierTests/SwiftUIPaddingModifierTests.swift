import XCTest
@testable import SwiftUIPaddingModifier

final class SwiftUIPaddingModifierTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIPaddingModifier().text, "Hello, World!")
    }
}
